<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("../partials/head") %>
  </head>
  <body>
    <div class="container">
      <div class="text-container">
        <h1 class="">HURRAY! You're Authenticated</h1>
      </div>
      <div class="">
        <button class="" id="logout">Logout</button>
      </div>
    </div>

    <script>
      // Logout
      document.querySelector('#logout').addEventListener('click', async e => {
        let res = await fetch('/api/auth/signout', { method: 'DELETE' })
        console.log(res)
      })
    </script>
  </body>
</html>
